# =============================================================================
# Inventory service configuration
# =============================================================================

quarkus.application.name=inventory-service
quarkus.http.port=8081

# -----------------------------------------------------------------------------
# gRPC server - port aligns with Order's quarkus.grpc.clients.inventory-grpc.port
# -----------------------------------------------------------------------------
quarkus.grpc.server.port=9001
quarkus.grpc.server.host=0.0.0.0

# -----------------------------------------------------------------------------
# OpenTelemetry / OTLP
# -----------------------------------------------------------------------------
quarkus.otel.exporter.otlp.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}
quarkus.otel.service.name=inventory-service
quarkus.otel.resource.attributes=service.namespace=workshop,deployment.environment=workshop
quarkus.otel.traces.sampler=parentbased_always_on
quarkus.otel.metrics.enabled=true

# -----------------------------------------------------------------------------
# Structured logging
# -----------------------------------------------------------------------------
quarkus.log.console.json.enabled=true
quarkus.log.console.json.pretty-print=false
quarkus.log.console.json.additional-field."service.name".value=${quarkus.application.name}
quarkus.log.console.json.additional-field."service.namespace".value=workshop

# -----------------------------------------------------------------------------
# Micrometer / Prometheus
# -----------------------------------------------------------------------------
quarkus.micrometer.binder.http-server.enabled=true
quarkus.micrometer.binder.grpc-server.enabled=true
quarkus.micrometer.binder.jvm=true
quarkus.micrometer.binder.system=true

# -----------------------------------------------------------------------------
# Stock simulation behavior
# -----------------------------------------------------------------------------
# A simple deterministic stock simulator drives the workshop scenarios. The
# stock store is in-memory and behaves as follows by default:
#   - SKUs starting "OUT_" are always unavailable
#   - SKUs starting "PARTIAL_" return PARTIAL status
#   - All other SKUs reserve successfully
# This is enough for Module 4's debugging exercise to have predictable
# failure injection without needing real persistent state.
workshop.inventory.simulator.partial-prefix=PARTIAL_
workshop.inventory.simulator.out-of-stock-prefix=OUT_
